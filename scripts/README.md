# 🚀 Система автоматизации развертывания DiamondBridge

Полная система автоматизации для разработки, тестирования и развертывания приложений с поддержкой Vercel.

## 📁 Структура проекта

```
├── scripts/                     # Скрипты автоматизации
│   ├── deploy.sh               # 🚀 Развертывание на Vercel
│   ├── setup.sh                # ⚙️  Первоначальная настройка
│   ├── test.sh                 # 🧪 Тестирование
│   └── backup.sh               # 💾 Резервное копирование
├── .github/workflows/          # GitHub Actions CI/CD
│   ├── ci.yml                  # Основной CI/CD pipeline
│   └── daily-tasks.yml         # Ежедневные задачи
├── package.json                # NPM скрипты и зависимости
├── backups/                    # Резервные копии (создается автоматически)
├── logs/                       # Логи системы (создается автоматически)
└── README.md                   # Эта документация
```

## 🎯 Быстрый старт

### 1. Первоначальная настройка

```bash
# Сделать скрипты исполняемыми
chmod +x scripts/*.sh

# Запустить настройку проекта
./scripts/setup.sh
```

Скрипт `setup.sh` автоматически:
- ✅ Проверит и установит Node.js (если нужно)
- 📦 Установит глобальные пакеты (Vercel CLI, ESLint, Prettier)
- 📁 Создаст структуру папок проекта
- ⚙️  Создаст конфигурационные файлы (.gitignore, .env.example, .editorconfig)
- 🔄 Инициализирует Git репозиторий
- 🪝 Настроит pre-commit hooks

### 2. Настройка переменных окружения

```bash
# Скопировать пример конфигурации
cp .env.example .env

# Отредактировать переменные
nano .env
```

Основные переменные:
```bash
# Vercel конфигурация
VERCEL_TOKEN=your-vercel-token
VERCEL_ORG_ID=your-vercel-org-id
VERCEL_PROJECT_ID=your-vercel-project-id

# Уведомления
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK
EMAIL_RECIPIENTS=admin@yourcompany.com

# Безопасность
BACKUP_ENCRYPTION_PASSPHRASE=your-encryption-passphrase
```

### 3. Развертывание

```bash
# Стандартное развертывание в production
npm run deploy

# Развертывание в staging окружение
npm run deploy:staging

# Развертывание в development окружение
npm run deploy:dev

# Прямое развертывание на Vercel
npm run deploy:preview
npm run deploy:prod
```

## 🔧 Подробное описание скриптов

### 🚀 scripts/deploy.sh

**Назначение:** Автоматическое развертывание приложения на Vercel

**Использование:**
```bash
./scripts/deploy.sh [имя_проекта] [окружение] [ветка]

# Примеры:
./scripts/deploy.sh diamondbridge production main
./scripts/deploy.sh diamondbridge staging develop
./scripts/deploy.sh myapp development feature-branch
```

**Функции:**
- 🔍 Проверка зависимостей (Node.js, npm, Vercel CLI)
- 🌿 Проверка Git ветки
- 🔍 Линтинг кода
- 🧪 Запуск тестов
- 🔨 Сборка проекта
- 💾 Создание резервной копии
- 🚀 Развертывание на Vercel
- 🧹 Очистка старых развертываний
- 📤 Уведомления в Slack

### ⚙️  scripts/setup.sh

**Назначение:** Первоначальная настройка проекта и окружения

**Использование:**
```bash
./scripts/setup.sh
```

**Функции:**
- 🔍 Проверка и установка Node.js
- 📦 Установка глобальных пакетов
- 📁 Создание структуры папок
- ⚙️  Создание конфигурационных файлов
- 📚 Создание README.md
- 🔄 Инициализация Git
- 🪝 Настройка pre-commit hooks

### 🧪 scripts/test.sh

**Назначение:** Комплексная система тестирования

**Использование:**
```bash
./scripts/test.sh [тип_тестов] [покрытие] [браузер] [подробный] [watch] [fail_fast]

# Примеры:
./scripts/test.sh                    # Все тесты
./scripts/test.sh unit               # Только unit тесты
./scripts/test.sh e2e true chrome    # E2E тесты с покрытием в Chrome
./scripts/test.sh lint               # Только линтинг
./scripts/test.sh --help             # Справка
```

**Типы тестов:**
- `all` - Все тесты (по умолчанию)
- `unit` - Unit тесты (Jest)
- `integration` - Integration тесты
- `e2e` - End-to-end тесты (Playwright)
- `lint` - ESLint проверки
- `type-check` - TypeScript проверки

**Особенности:**
- 📊 Генерация отчетов о покрытии
- 🌐 Поддержка множества браузеров для E2E тестов
- 🔄 Режим наблюдения (watch mode)
- ⚡ Остановка при первой ошибке
- 📁 Сохранение результатов в logs/

### 💾 scripts/backup.sh

**Назначение:** Система резервного копирования

**Использование:**
```bash
# Создание бэкапа
./scripts/backup.sh [тип] [имя] [сжатие] [шифрование] [облако]

# Дополнительные команды
./scripts/backup.sh list                    # Список бэкапов
./scripts/backup.sh restore путь/к/файлу    # Восстановление
./scripts/backup.sh verify путь/к/файлу     # Проверка целостности
./scripts/backup.sh cleanup                 # Очистка старых бэкапов
```

**Типы бэкапов:**
- `auto` - Автоматическое определение
- `full` - Полный бэкап всего проекта
- `code` - Только исходный код
- `database` - Только база данных
- `config` - Только конфигурационные файлы

**Функции:**
- 🗜️  Сжатие архивов (tar.gz)
- 🔐 Шифрование (GPG)
- ☁️  Синхронизация с облаком (rclone)
- 🧹 Автоматическая очистка старых копий
- 📋 Создание манифестов
- 🔍 Проверка целостности

## 📦 NPM скрипты

### Основные команды
```bash
npm run dev                    # Запуск в режиме разработки
npm run build                  # Сборка проекта
npm start                      # Запуск production сервера
```

### Тестирование
```bash
npm test                       # Все тесты
npm run test:unit             # Unit тесты
npm run test:integration      # Integration тесты
npm run test:e2e              # E2E тесты
npm run test:watch            # Тесты в режиме наблюдения
npm run test:coverage         # Тесты с покрытием
npm run test:lint             # Только линтинг
npm run test:type-check       # Проверка типов
```

### Качество кода
```bash
npm run lint                  # ESLint проверка
npm run lint:fix             # Исправление ESLint ошибок
npm run format               # Форматирование Prettier
npm run format:check         # Проверка форматирования
npm run type-check           # Проверка TypeScript
```

### Развертывание
```bash
npm run deploy               # Развертывание в production
npm run deploy:staging       # Развертывание в staging
npm run deploy:dev           # Развертывание в development
npm run deploy:preview       # Предпросмотр развертывания
npm run deploy:prod          # Прямое развертывание на Vercel
```

### Резервное копирование
```bash
npm run backup              # Автоматический бэкап
npm run backup:full         # Полный бэкап
npm run backup:code         # Бэкап кода
npm run backup:database     # Бэкап базы данных
npm run backup:cleanup      # Очистка старых бэкапов
npm run backup:list         # Список бэкапов
npm run backup:restore      # Восстановление из бэкапа
```

### Утилиты
```bash
npm run setup               # Первоначальная настройка
npm run clean               # Очистка временных файлов
npm run clean:all           # Полная очистка
npm run build:analyze       # Анализ размера bundle
```

## 🔄 GitHub Actions CI/CD

### Основной pipeline (ci.yml)

**Триггеры:**
- Push в ветки: `main`, `develop`, `staging`
- Pull Request в `main`

**Jobs:**
1. **🔍 code-quality** - Проверка качества кода
   - ESLint, Prettier, TypeScript
   - Анализ Python кода (если есть)

2. **🧪 test** - Тестирование
   - Unit и Integration тесты
   - Поддержка Node.js 16, 18, 20
   - Загрузка отчета о покрытии в Codecov

3. **🌐 e2e-test** - End-to-End тесты
   - Playwright тесты в различных браузерах

4. **🔨 build** - Сборка проекта
   - Создание build artifacts

5. **💾 backup** - Резервное копирование
   - Автоматический бэкап перед развертыванием

6. **🚀 deploy** - Развертывание
   - Автоматическое развертывание на Vercel

7. **📢 notify** - Уведомления
   - Slack и Email уведомления

8. **🔒 security** - Проверка безопасности
   - Snyk анализ уязвимостей
   - npm audit

9. **📚 docs** - Документация
   - Генерация JSDoc
   - Обновление статистики в README

### Ежедневные задачи (daily-tasks.yml)

**Триггеры:**
- Ежедневно в 02:00 UTC
- Ручной запуск

**Jobs:**
1. **💾 daily-backup** - Ежедневное резервное копирование
2. **🏥 system-health-check** - Проверка состояния системы
3. **🔄 dependency-update** - Обновление зависимостей
4. **🧹 cleanup** - Очистка и оптимизация
5. **📈 project-status-report** - Ежедневный отчет

## 🔐 Настройка секретов в GitHub

В настройках репозитория добавьте следующие секреты:

### Основные секреты
```
VERCEL_TOKEN              # Токен Vercel
VERCEL_ORG_ID            # ID организации Vercel
VERCEL_PROJECT_ID        # ID проекта Vercel
```

### Уведомления
```
SLACK_WEBHOOK_URL       # Webhook URL для Slack
EMAIL_RECIPIENTS        # Email адреса для уведомлений
SMTP_USER               # SMTP пользователь
SMTP_PASS               # SMTP пароль
SMTP_HOST               # SMTP сервер
SMTP_PORT               # SMTP порт
```

### Безопасность и анализ
```
SNYK_TOKEN             # Токен Snyk для проверки уязвимостей
RCLONE_REMOTE          # Удаленное хранилище для rclone
```

### Получение Vercel токена
1. Зайдите на [vercel.com](https://vercel.com/account/tokens)
2. Создайте новый токен
3. Добавьте его в GitHub Secrets

## 📊 Мониторинг и логирование

### Структура логов
```
logs/
├── test-results/          # Результаты тестирования
├── deploy-YYYYMMDD-HHMMSS.log  # Логи развертывания
├── backup-history.log     # История бэкапов
└── system-health-report.md # Отчеты о состоянии
```

### Мониторинг
- 📊 Автоматические отчеты о состоянии проекта
- 📈 Статистика коммитов и активности
- 📦 Мониторинг зависимостей
- 🔒 Проверки безопасности
- 💾 Контроль резервных копий

## 🛠️  Устранение неисправностей

### Часто встречающиеся проблемы

#### 1. Ошибки прав доступа к скриптам
```bash
chmod +x scripts/*.sh
```

#### 2. Ошибки зависимостей Node.js
```bash
# Очистка кэша npm
npm cache clean --force
rm -rf node_modules
npm install

# Переустановка через setup
./scripts/setup.sh
```

#### 3. Ошибки Vercel развертывания
```bash
# Проверка настроек Vercel
vercel login
vercel link

# Проверка переменных окружения
vercel env ls
```

#### 4. Ошибки тестирования
```bash
# Полная переустановка Playwright
npx playwright install --force

# Очистка Jest кэша
npm test -- --clearCache
```

#### 5. Проблемы с бэкапами
```bash
# Проверка места на диске
df -h

# Проверка прав доступа к папке backups
ls -la backups/

# Проверка настроек rclone
rclone config
```

## 📝 Примеры рабочих процессов

### Разработка новой функции
```bash
1. git checkout -b feature/new-feature
2. npm run test:watch          # Разработка с тестами
3. npm run lint:fix            # Исправление ошибок
4. git add . && git commit -m "feat: add new feature"
5. git push origin feature/new-feature
6. Создание Pull Request
7. Автоматический CI/CD через GitHub Actions
8. После approval - merge в main
9. Автоматическое развертывание
```

### Экстренное исправление
```bash
1. git checkout main
2. git checkout -b hotfix/critical-fix
3. ./scripts/test.sh lint      # Быстрая проверка
4. git add . && git commit -m "fix: critical bug"
5. git push origin hotfix/critical-fix
6. npm run deploy              # Быстрое развертывание
7. Создание Pull Request для code review
```

### Плановое обновление
```bash
1. ./scripts/backup.sh full    # Создание бэкапа
2. npm run backup:cleanup      # Очистка старых копий
3. npm update                  # Обновление зависимостей
4. npm test                    # Полное тестирование
5. npm run deploy:staging      # Развертывание в staging
6. npm run test:e2e            # E2E тесты в staging
7. npm run deploy              # Развертывание в production
```

## 🚀 Расширение функционала

### Добавление новых проверок
Редактируйте файлы в `.github/workflows/` для добавления новых шагов CI/CD.

### Создание кастомных скриптов
Добавляйте новые скрипты в папку `scripts/` и регистрируйте их в `package.json`.

### Интеграция с другими сервисами
Расширяйте функционал для интеграции с:
- 🐳 Docker и Kubernetes
- 📊 Мониторинговыми системами (Prometheus, Grafana)
- 🔍 Дополнительными инструментами анализа кода
- ☁️  Другими облачными платформами

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь, что все переменные окружения настроены
3. Проверьте права доступа к файлам и папкам
4. Обратитесь к документации используемых инструментов

---

**🎉 Удачной разработки и развертывания с DiamondBridge!**