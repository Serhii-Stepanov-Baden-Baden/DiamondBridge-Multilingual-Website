# Руководство по Внесению Вкладов (CONTRIBUTING.md)

Благодарим за ваш интерес к участию в разработке DiamondBridge! Это руководство поможет вам начать работу с проектом и внести свой вклад в его развитие.

## 📋 Содержание

- [Кодекс Поведения](#кодекс-поведения)
- [Как Внести Вклад](#как-внести-вклад)
- [Среда Разработки](#среда-разработки)
- [Стандарты Кода](#стандарты-кода)
- [Процесс Разработки](#процесс-разработки)
- [Тестирование](#тестирование)
- [Документация](#документация)
- [Сообщения об Ошибках](#сообщения-об-ошибках)
- [Запросы Функций](#запросы-функций)
- [Проверка Pull Request](#проверка-pull-request)

## 🌟 Кодекс Поведения

Мы стремимся создать открытое и дружелюбное сообщество. Все участники должны соблюдать наш [Кодекс Поведения](CODE_OF_CONDUCT.md).

### Наши Принципы

- **Уважение** ко всем участникам
- **Конструктивная** обратная связь
- **Открытость** к различным точкам зрения
- **Сотрудничество** и взаимопомощь
- **Профессионализм** в общении

## 🤝 Как Внести Вклад

Существует множество способов внести вклад в проект:

### 🐛 Исправление Ошибок
- Сообщите об ошибке через Issues
- Исправите ошибку и отправьте Pull Request
- Улучшите тесты для предотвращения регрессий

### ✨ Добавление Функций
- Предложите новую функцию через Feature Request
- Реализуйте согласованную функциональность
- Добавьте соответствующие тесты и документацию

### 📚 Улучшение Документации
- Исправьте опечатки и грамматические ошибки
- Добавьте примеры использования
- Создайте руководства для новых пользователей

### 🎨 Улучшение UX/UI
- Предложите улучшения интерфейса
- Создайте новые компоненты
- Улучшите доступность приложения

### 🔧 Техническая Долг
- Рефакторинг кода
- Оптимизация производительности
- Обновление зависимостей

## 💻 Среда Разработки

### Системные Требования

- **Node.js** 16.0+
- **npm** 8.0+ или **pnpm** 8.0+
- **MongoDB** 5.0+
- **Redis** 6.0+
- **Git** 2.30+

### Установка

1. **Форкните репозиторий**
```bash
git clone https://github.com/YOUR_USERNAME/diamondbridge.git
cd diamondbridge
```

2. **Установите зависимости Backend**
```bash
cd backend
npm install
```

3. **Установите зависимости Frontend**
```bash
cd ../diamondbridge-multilingual
pnpm install
```

4. **Настройте переменные окружения**
```bash
cd ../backend
cp .env.example .env
# Отредактируйте .env файл
```

### Полезные Команды

```bash
# Запуск в development
npm run dev              # Backend
pnpm dev                 # Frontend

# Линтинг
npm run lint             # Backend
pnpm lint                # Frontend

# Тестирование
npm test                 # Backend
pnpm test                # Frontend

# Сборка
npm run build            # Backend
pnpm build               # Frontend
```

## 📝 Стандарты Кода

### Общие Принципы

- **KISS** - Keep It Simple, Stupid
- **DRY** - Don't Repeat Yourself  
- **SOLID** принципы
- **Читаемость** важнее краткости
- **Комментарии** для сложной логики

### Backend (Node.js)

#### Структура Файлов
```
backend/
├── config/              # Конфигурация
├── routes/              # API маршруты
├── services/            # Бизнес-логика
├── middleware/          # Express middleware
├── utils/               # Вспомогательные функции
└── tests/               # Тесты
```

#### Стиль Кода
```javascript
// ✅ Хорошо: Используйте async/await
async function getUser(id) {
  try {
    const user = await User.findById(id);
    return user;
  } catch (error) {
    logger.error('Error getting user:', error);
    throw error;
  }
}

// ❌ Плохо: Callback hell
function getUser(id, callback) {
  User.findById(id, (err, user) => {
    if (err) return callback(err);
    callback(null, user);
  });
}
```

#### Нейминг
- **Файлы**: camelCase (userService.js)
- **Функции**: camelCase (getUserById)
- **Константы**: UPPER_SNAKE_CASE (API_VERSION)
- **Классы**: PascalCase (UserService)

### Frontend (React + TypeScript)

#### Структура Компонентов
```typescript
// ✅ Хорошо: Функциональные компоненты с хуками
interface UserCardProps {
  user: User;
  onEdit: (id: string) => void;
}

export const UserCard: FC<UserCardProps> = ({ user, onEdit }) => {
  return (
    <div className="user-card">
      <h3>{user.name}</h3>
      <button onClick={() => onEdit(user.id)}>
        Редактировать
      </button>
    </div>
  );
};
```

#### Использование TypeScript
```typescript
// ✅ Хорошо: Строгая типизация
interface AIRequest {
  prompt: string;
  provider: 'openai' | 'anthropic' | 'google';
  options?: {
    model?: string;
    maxTokens?: number;
    temperature?: number;
  };
}

// ❌ Плохо: any типы
function processRequest(request: any) {
  // ...
}
```

### CSS/SCSS

#### BEM Методология
```scss
// ✅ Хорошо: BEM naming
.user-card {
  // styles
  
  &__header {
    // styles
  }
  
  &__title {
    // styles
    
    &--highlighted {
      // styles
    }
  }
  
  &__button {
    // styles
    
    &:hover {
      // styles
    }
  }
}
```

## 🔄 Процесс Разработки

### 1. Создание Feature Branch

```bash
# Обновите main branch
git checkout main
git pull upstream main

# Создайте feature branch
git checkout -b feature/descriptive-name
# или
git checkout -b fix/bug-description
# или
git checkout -b docs/update-readme
```

### 2. Разработка

```bash
# Следите за изменениями
git add .
git commit -m "feat: add user authentication

- Implement JWT authentication
- Add login/logout endpoints  
- Create protected route middleware

Closes #123"
```

### 3. Commit Сообщения

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```bash
# Формат
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

#### Типы коммитов:
- **feat**: новая функциональность
- **fix**: исправление ошибки
- **docs**: изменения в документации
- **style**: форматирование кода
- **refactor**: рефакторинг
- **test**: добавление тестов
- **chore**: обновление зависимостей

#### Примеры:
```bash
feat(auth): add JWT token refresh
fix(ai): resolve OpenAI API timeout issue
docs(api): update endpoint documentation
style(components): format UserCard component
refactor(services): simplify AI service logic
test(backend): add user authentication tests
chore(deps): update to Node.js 18
```

### 4. Отправка Pull Request

```bash
# Запушьте branch
git push origin feature/your-feature-name

# Создайте Pull Request через GitHub интерфейс
```

#### Описание Pull Request

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] 🐛 Исправление ошибки
- [ ] ✨ Новая функциональность  
- [ ] 💥 Breaking change
- [ ] 📚 Документация
- [ ] 🎨 Стиль кода
- [ ] ♻️ Рефакторинг
- [ ] ✅ Тесты
- [ ] 🔧 Сборка

## Как Тестировалось
- [ ] Локально с помощью `npm run dev`
- [ ] Единичные тесты
- [ ] Интеграционные тесты
- [ ] Ручное тестирование

## Скриншоты (если применимо)

## Checklist
- [ ] Код проходит линтинг
- [ ] Все тесты проходят
- [ ] Добавлены тесты для новой функциональности
- [ ] Документация обновлена
- [ ] Коммит сообщения соответствуют conventional commits
```

## 🧪 Тестирование

### Backend Тестирование

#### Структура Тестов
```bash
backend/tests/
├── unit/              # Юнит тесты
├── integration/       # Интеграционные тесты
├── fixtures/          # Тестовые данные
└── helpers/           # Вспомогательные функции
```

#### Написание Тестов
```javascript
// tests/unit/ai-service.test.js
const aiService = require('../../services/ai-service');
const { mockOpenAI } = require('../fixtures/ai-providers');

describe('AI Service', () => {
  describe('generateText', () => {
    it('should generate text with OpenAI', async () => {
      // Arrange
      const request = {
        prompt: 'Напиши стихотворение',
        provider: 'openai',
        options: { model: 'gpt-4' }
      };

      // Act
      const result = await aiService.generateText(request);

      // Assert
      expect(result).toHaveProperty('text');
      expect(result.provider).toBe('openai');
      expect(result.text).toBeTruthy();
    });

    it('should handle API errors gracefully', async () => {
      // Тест обработки ошибок
    });
  });
});
```

#### Запуск Тестов
```bash
npm test                    # Все тесты
npm run test:watch         # Watch режим
npm run test:coverage      # С покрытием
npm run test:integration   # Только интеграционные
```

### Frontend Тестирование

#### React Testing Library
```typescript
// src/components/__tests__/UserCard.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { UserCard } from '../UserCard';

const mockUser = {
  id: '1',
  name: 'Иван Иванов',
  email: 'ivan@example.com'
};

describe('UserCard', () => {
  it('should render user information', () => {
    render(<UserCard user={mockUser} onEdit={jest.fn()} />);
    
    expect(screen.getByText('Иван Иванов')).toBeInTheDocument();
    expect(screen.getByText('ivan@example.com')).toBeInTheDocument();
  });

  it('should call onEdit when edit button is clicked', () => {
    const onEdit = jest.fn();
    render(<UserCard user={mockUser} onEdit={onEdit} />);
    
    fireEvent.click(screen.getByText('Редактировать'));
    expect(onEdit).toHaveBeenCalledWith('1');
  });
});
```

#### E2E Тестирование с Playwright
```typescript
// e2e/ai-chat.spec.ts
import { test, expect } from '@playwright/test';

test('AI chat functionality', async ({ page }) => {
  await page.goto('/ai-chat');
  
  await page.fill('[data-testid="prompt-input"]', 'Привет, как дела?');
  await page.click('[data-testid="send-button"]');
  
  await expect(page.locator('[data-testid="ai-response"]')).toBeVisible();
});
```

## 📚 Документация

### Комментарии к Коду

```typescript
/**
 * Генерирует текст используя указанный AI провайдер
 * 
 * @param request - Параметры запроса к AI
 * @param request.prompt - Текстовый промпт для генерации
 * @param request.provider - AI провайдер (openai|anthropic|google)
 * @param request.options - Дополнительные параметры
 * @returns Promise с результатом генерации
 * @throws {AIError} При ошибке API провайдера
 */
async function generateText(request: AIRequest): Promise<AIResponse> {
  // implementation
}
```

### README Обновления

При добавлении новой функциональности:

1. **Обновите основные возможности** в README.md
2. **Добавьте примеры использования** в соответствующих секциях
3. **Обновите схему архитектуры** при изменении структуры
4. **Добавьте скриншоты** для UI изменений

### API Документация

#### Swagger Комментарии
```javascript
/**
 * @swagger
 * /api/ai/generate:
 *   post:
 *     summary: Генерация текста с помощью AI
 *     description: Генерирует текст используя различные AI провайдеры
 *     tags: [AI]
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             $ref: '#/components/schemas/AIRequest'
 *     responses:
 *       200:
 *         description: Успешная генерация текста
 *         content:
 *           application/json:
 *             schema:
 *               $ref: '#/components/schemas/AIResponse'
 *       400:
 *         description: Неверные параметры запроса
 */
app.post('/api/ai/generate', aiController.generate);
```

## 🐛 Сообщения об Ошибках

### Создание Issue

```markdown
## 🐛 Описание Ошибки
Краткое описание того, что произошло

## 🔄 Для Воспроизведения
Шаги для воспроизведения:
1. Перейдите на '...'
2. Нажмите на '....'
3. Прокрутите до '....'
4. Ошибка

## ✅ Ожидаемое Поведение
Что должно было произойти

## 📸 Скриншоты
Если применимо, добавьте скриншоты

## 🖥️ Окружение
- OS: [например macOS 12.0]
- Browser: [например Chrome 96.0]
- Node.js: [например 16.0.0]
- Версия приложения: [например 1.0.0]

## 📋 Дополнительная Информация
Любая другая информация об ошибке
```

### Приоритеты Ошибок

- **🔴 Критические** - приложение не работает
- **🟡 Важные** - серьезные проблемы функциональности
- **🟢 Минорные** - небольшие проблемы UI/UX
- **🔵 Улучшения** - предложения для улучшения

## ✨ Запросы Функций

### Feature Request Template

```markdown
## 💡 Описание Функции
Четкое описание предлагаемой функциональности

## 🎯 Проблема
Какую проблему решает эта функция?

## 💭 Предлагаемое Решение
Описание того, как вы видите решение

## 🔄 Альтернативы
Другие способы решения проблемы

## 📋 Дополнительная Информация
Любые другие комментарии или скриншоты
```

## 🔍 Проверка Pull Request

### Автоматические Проверки

- **✅ Linting** - код соответствует стандартам
- **🧪 Tests** - все тесты проходят
- **🔒 Security** - нет уязвимостей безопасности
- **📊 Coverage** - покрытие тестами не ниже 80%

### Ручная Проверка

#### Код Качество
```bash
# Проверьте линтинг
npm run lint

# Запустите тесты
npm test

# Проверьте безопасность
npm audit
```

#### Функциональность
1. **Протестируйте локально** все изменения
2. **Проверьте edge cases** и обработку ошибок
3. **Убедитесь в обратной совместимости**
4. **Проверьте производительность** для больших изменений

### Проверочный Список

Для каждого Pull Request проверьте:

- [ ] Код следует стандартам проекта
- [ ] Коммит сообщения понятны и информативны
- [ ] Добавлены соответствующие тесты
- [ ] Документация обновлена
- [ ] Нет console.log и debugger statements
- [ ] Все тесты проходят
- [ ] Покрытие тестами достаточное
- [ ] Нет предупреждений линтинга
- [ ] Безопасность не нарушена
- [ ] Производительность не ухудшилась

## 🚀 Развертывание

### Среда Разработки

```bash
# Backend
cd backend
npm run dev

# Frontend  
cd diamondbridge-multilingual
pnpm dev
```

### Staging

```bash
# Сборка и деплой
npm run build:staging
npm run deploy:staging
```

### Production

```bash
# Полная сборка
npm run build:prod
npm run deploy:prod
```

## 📞 Получение Помощи

Если у вас есть вопросы по разработке:

- **💬 Discord**: [Сервер разработки](https://discord.gg/diamondbridge)
- **📧 Email**: dev@diamondbridge.com  
- **📚 Документация**: [https://docs.diamondbridge.com](https://docs.diamondbridge.com)
- **🐛 Issues**: [GitHub Issues](https://github.com/your-org/diamondbridge/issues)

### Ресурсы

- [React Документация](https://reactjs.org/docs)
- [Node.js Best Practices](https://github.com/goldbergyoni/nodebestpractices)
- [TypeScript Handbook](https://www.typescriptlang.org/docs)
- [Testing Library](https://testing-library.com/docs)
- [Conventional Commits](https://www.conventionalcommits.org)

## 🏆 Признание

Мы ценим вклад каждого участника! Контрибьюторы будут:

- **Упомянуты** в README проекта
- **Перечислены** в релизных заметках
- **Получат доступ** к private development каналу
- **Возможно** приглашены в core team для активных контрибьюторов

### Hall of Fame

Контрибьюторы проекта будут отмечены в файле [CONTRIBUTORS.md](CONTRIBUTORS.md).

---

**Спасибо за ваш вклад в DiamondBridge!** 🎉

*Ваше участие делает проект лучше для всех.*